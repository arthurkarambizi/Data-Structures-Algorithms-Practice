/**
 * @param {number[][]} matrix
 * @return {void} Do not return anything, modify matrix in-place instead.
 */
var rotate = function (matrix) {
    let matrixArray = {};
    let count = 0;

    for (let index = 0; index < matrix.length; index++) {
        // console.log(count);
        // console.log(index);
		// count === matrix.length ? 0 : count++;
		//
	}


    // for (let index = 0; index < matrixCopy.length; index++) {
    // 	matrix[index] = matrixCopy[matrixCopy.length - count ].reverse()
    // 	count++;
    // }

    console.log(matrixArray);
};

let matrix = [
    [1, 2],
    [3, 4]
]; // output: [[3,1],[4,2]];
rotate(matrix);
